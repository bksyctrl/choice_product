# 🚀 AgentCenter v1.1.2 详细开发与架构设计文档

## 1. 概述 (Overview)
本文档基于 `1.1.2版本.md` 业务需求说明书编写，旨在指导研发团队完成后端接口、数据库设计、爬虫任务调度、AI 打分算法及前端交互的落地。
**核心业务目标**：围绕**印花工厂基建**与**POD按需定制**，通过跨品类数据抓取与 AI 自动化打分，挖掘具有高迁移价值的爆款视觉设计资产。

---

## 2. 版本边界与落地路径 (Boundaries & Path)
本项目当前基于轻量级技术栈运行，本版本的迭代应直接在现有架构上进行功能扩充，避免过度工程。

### 2.1 版本边界
*   **不做**：不破坏或重构现有 FastMoss、Kalodata 数据流；允许为 `fastmoss_product_aggregate`、`fastmoss_product_rank_aggregate`、`kalodata_youwei_product` 追加评分与插画状态扩展字段。
*   **不做**：不引入重型消息队列（如 Kafka/RabbitMQ）或重型调度框架，提取插画任务通过数据库状态轮询或简单线程池实现。
*   **聚焦**：跨品类 `pod_cross_category` 是新增重点；评分、销售环比、插画状态能力需要横向覆盖现有三张商品表。

### 2.2 核心开发约束
1.  **`category_id` 一致性**：新表中的 `category_l1`, `category_l2`, `category_l3` 必须与现有 JSON 树（如女装 `2`，箱包 `7` 等）的 `c_code` 完全保持一致，以便复用现有的前端级联筛选器。
2.  **列表接口性能红线 (不查大图)**：在实现 `/api/products` 列表查询时，**严禁**在 SELECT 语句中查询 `image_base64`, `sales_overview`, `ad_analysis` 等 `LONGTEXT` 字段。必须利用延迟关联仅拉取基础标量数据，详情内容仅在点击“站内详情”时按需获取。
3.  **后端框架**：沿用现有的 **Python (Flask)** (`app.py`)。
4.  **前端展现**：沿用现有的 **Vanilla JS + HTML** (`static/index.html`)。

---

## 3. 数据库设计 (Database Schema)

### 3.1 跨品类商品表 (`pod_cross_category_product`)
用于存储从 FastMoss 爬取的跨品类榜单数据。为确保系统底层向下兼容，本表结构完全以 `fastmoss_search_product` 为基础进行扩展，追加了 POD 核心字段（如销售环比、AI 打分等）。

| 字段名 | 类型 | 约束/说明 |
| :--- | :--- | :--- |
| `id` | BIGINT | PK, AUTO_INC, 主键 |
| `product_id` | BIGINT | 商品ID，与 `date_record` 构成联合唯一索引 |
| `date_record` | VARCHAR(10) | 日期 (如 '2026-06-08') |
| `title` | VARCHAR(500) | 商品标题 |
| `rank_sold_count` | INT | saleRank接口返回的sold_count，表示榜单销量/当前销量 |
| `rating` | DECIMAL(3,2) | 商品评分 |
| `region` | VARCHAR(10) | 销售地区 |
| `currency` | VARCHAR(10) | 货币 |
| `base_price` | DECIMAL(10,2)| 商品底价 |
| `real_price` | VARCHAR(50) | 实际售价 |
| `original_price`| VARCHAR(50) | 划线原价 |
| `launch_time` | DATETIME | 上架时间 |
| `author_count` | INT | 带货达人数 |
| `aweme_count` | INT | 短视频数 |
| `live_count` | INT | 直播场次 |
| `sold_count` | BIGINT | 累计销量 |
| `sale_amount` | DECIMAL(15,2)| 销售总额 |
| `review_count` | INT | 评价总数 |
| `commission_rate`| VARCHAR(20) | 佣金比例 |
| `category_l1` | VARCHAR(100) | 一级类目 |
| `category_l2` | VARCHAR(100) | 二级类目 |
| `category_l3` | VARCHAR(100) | 三级类目 |
| `stock_count` | INT | 库存数量 |
| `viral_index` | INT | 商品热度指数 |
| `popularity_index`| INT | 人气指数 |
| `country_rank` | VARCHAR(50) | 国家排名 |
| `category_rank` | VARCHAR(50) | 类目排名 |
| `region_name` | VARCHAR(50) | 地区名称 |
| `transport_fee` | VARCHAR(50) | 运费 |
| `detail_url` | TEXT | 商品详情链接 |
| `cover_list` | LONGTEXT | 商品封面图列表 |
| `channel_ratio` | TEXT | 近28天成交渠道占比 |
| `channel_gmv_ratio`| TEXT | 近28天成交渠道GMV占比 |
| `content_ratio` | TEXT | 近28天成交内容占比 |
| `投放_ratio` | TEXT | 近28天成交投放占比 |
| `sales_overview`| LONGTEXT | 近28天销售概览数据 |
| `fan_distribution`| LONGTEXT | 达人粉丝数量分布 |
| `author_type_distribution`| LONGTEXT | 达人类型分布 |
| `ad_analysis` | LONGTEXT | 投流分析 |
| `comments` | TEXT | 商品评论 |
| `image_base64` | LONGTEXT | 商品主图_base64 |
| `overview_7d` | LONGTEXT | 近7天销售概览 |
| `distribution_7d`| LONGTEXT | 近7天成交内容占比 |
| `overview_90d` | LONGTEXT | 近90天销售概览 |
| `distribution_90d`| TEXT | 近90天成交内容占比 |
| `overview_180d` | LONGTEXT | 近180天销售概览 |
| `distribution_180d`| TEXT | 近180天成交内容占比 |
| `ad_analysis_7d`| LONGTEXT | 近7天投流分析 |
| `ad_analysis_90d`| LONGTEXT | 近90天投流分析 |
| `ai_learned` | INT | AI深度学习标记 (默认0) |
| `sku_analysis_7d`| LONGTEXT | 近7天sku分析 |
| `sku_analysis_28d`| LONGTEXT | 近28天sku分析 |
| **`sales_mom`** | DECIMAL(5,2)| **[POD 新增]** 销售环比 (用于趋势洞察) |
| **`score`** | DECIMAL(5,2)| **[POD 新增]** AI 综合得分 (按公式回算) |
| **`status`** | TINYINT | **[POD 新增]** 插画提取状态字典值 (见 4.4 节) |

> **注**：必须在 `category_l1`, `category_l2`, `category_l3`, `score`, `status`, `date_record` 上建立索引以支持高速分页与过滤。查询必须采用**延迟关联**。

### 3.2 异步任务执行表 (`pod_illustration_task`)
为避免长时间阻塞和方便重试，新增独立的插画提取任务表。

| 字段名 | 类型 | 备注 |
| :--- | :--- | :--- |
| `task_id` | VARCHAR(64) | PK, 任务唯一流水号 |
| `product_id` | BIGINT | 关联商品 |
| `style_prompt` | VARCHAR(255) | 选定的视觉风格词 |
| `original_image_url`| VARCHAR(512) | 原图链接 |
| `result_image_url` | VARCHAR(512) | AI抠图/重绘后的图床链接 |
| `status` | TINYINT | 任务执行状态: 0:排队中, 1:处理中, 2:成功, 3:失败 |
| `error_msg` | TEXT | 失败原因 |
| `created_at` | DATETIME | |
| `updated_at` | DATETIME | |

---

## 4. 核心任务逻辑、评分字段与状态定义

> 本章仍以现有 **Flask / `app.py` + `static/index.html`** 架构为落地基础，不切换到 Java AgentCenter。爬取数据本身由外部脚本或第三方人员处理，本系统只负责数据入库后的统一展示、评分、筛选和插画任务管理。

### 4.1 多数据源统一评分范围

评分能力不只服务 `pod_cross_category_product`，而是同时覆盖当前选品系统内的四类商品表：

| 数据源 | 表名 | 是否增加评分能力 | 说明 |
| :--- | :--- | :--- | :--- |
| 统一表 | `fastmoss_product_aggregate` | 是 | 当前主商品池，需支持综合打分、销售环比、插画任务状态 |
| FastMoss 榜单表 | `fastmoss_product_rank_aggregate` | 是 | 榜单型商品池，需支持同一套评分字段 |
| Kalodata | `kalodata_youwei_product` | 是 | 已有属性、卖点、SKU 等字段，也纳入评分与插画状态 |
| 跨品类 | `pod_cross_category_product` | 是 | 新增跨品类热品池，作为重点实验场景 |

### 4.2 数据库新增字段建议

为了让列表页、筛选器、排序、详情页和插画任务状态都能统一工作，上述四张商品表建议补充同一组字段。字段名保持英文，避免前端和后端为不同数据源重复写特殊判断。

| 字段名 | 类型建议 | 含义 | 使用位置 |
| :--- | :--- | :--- | :--- |
| `selection_score` | DECIMAL(6,2) DEFAULT NULL | 综合选品得分，0~100 分 | 列表排序、筛选、卡片展示 |
| `score_reason` | TEXT DEFAULT NULL | 得分摘要，说明为什么分高/分低 | 详情页解释 |
| `sales_mom` | DECIMAL(8,2) DEFAULT NULL | 销售环比百分比，例如 `15.50` 表示增长 15.5% | 趋势判断、排序 |
| `illustration_status` | TINYINT DEFAULT 0 | 插画提取状态，0~4 | 列表按钮、详情页任务状态 |
| `illustration_result_url` | VARCHAR(512) DEFAULT NULL | 插画提取/重绘结果图 | 详情页展示 |

索引建议：

```sql
CREATE INDEX idx_score_date ON <table_name>(date_record, selection_score);
CREATE INDEX idx_mom_date ON <table_name>(date_record, sales_mom);
CREATE INDEX idx_illustration_status ON <table_name>(illustration_status);
```

> `illustration_status` 不复用原有 `audit_status`。`audit_status` 是人工审核/上架流程状态，`illustration_status` 是素材提取任务状态，两者业务含义不同，必须分开。

### 4.3 `sales_mom` 定义

`sales_mom` 是“销售环比”，用于衡量当前周期销量相对上一周期的变化幅度。

推荐定义：

```text
sales_mom = ((当前周期销量 - 上一周期销量) / 上一周期销量) * 100
```

示例：

| 当前周期销量 | 上一周期销量 | sales_mom |
| :--- | :--- | :--- |
| 120 | 100 | 20.00 |
| 80 | 100 | -20.00 |
| 0 | 100 | -100.00 |
| 100 | 0 | NULL 或 100.00，需按业务规则单独处理 |

特殊情况建议：

1. 上一周期销量为 0 且当前周期销量也为 0：`sales_mom = 0`
2. 上一周期销量为 0 且当前周期销量大于 0：`sales_mom = 100`，并在 `score_reason` 中标记为“新增长商品”
3. 当前周期销量可按用户选择的采集日期范围求和；如果用户没有选择日期，默认取该表最新 `date_record` 的数据。

### 4.4 商品筛选与评分逻辑

建议在 `app.py` 或 `tools/score_service.py` 中新增轻量评分服务，不引入复杂框架。服务职责如下：

1. **字段归一化**：不同表字段不同，需要先统一成内部结构，例如 `price`、`rating`、`sales_7d`、`sales_30d`、`total_sales`、`video_count`、`category_id`。
2. **风控拦截**：命中风控规则的商品直接 `selection_score = 0`，并在 `score_reason` 中说明原因。
3. **计算 `sales_mom`**：按照当前采集日期范围和上一等长周期计算销售环比。
4. **计算 `selection_score`**：按销量分、评分分、热度分、趋势分四个维度求和。
5. **写入数据库字段**：把 `selection_score`、`score_reason`、`sales_mom`、`illustration_status`、`illustration_result_url` 回写到对应商品表。

#### 4.4.1 综合得分公式

```text
综合得分 = 销量分 + 评分分 + 热度分 + 趋势分
满分 100，各维度满分：销量 35 + 评分 25 + 热度 25 + 趋势 15
```

#### 4.4.2 各维度公式

**1. 销量分（权重 35%）**

```javascript
const norm30d = log10(30天销量 + 1) / log10(类目最大30天销量 + 1); // 0~1
const penalty = Math.abs(norm30d - 0.5) * 0.67;
const 销量分 = norm30d * (1 - penalty) * 2 * 35;
```

**2. 评分分（权重 25%）**

```javascript
const safeRating = 评分 || 3.5;
const 评分分 = (safeRating / 5) * 25;
```

> 说明：业务表达中“`(评分 / 5) × 100 × 25`”代表百分制乘 25% 权重，代码落地应写成 `(评分 / 5) * 25`，否则评分分会超过 25 分。

**3. 热度分（权重 25%）**

```javascript
const normVideo = log10(视频数 + 1) / log10(类目最大视频数 + 1); // 0~1
const penalty = Math.abs(normVideo - 0.5) * 0.67;
const 热度分 = normVideo * (1 - penalty) * 2 * 25;
```

**4. 趋势分（权重 15%）**

```javascript
const norm7d = log10(7天销量 + 1) / log10(类目最大7天销量 + 1);
const norm30d = log10(30天销量 + 1) / log10(类目最大30天销量 + 1);
const ratio = norm30d > 0 ? norm7d / norm30d : 0;
const 趋势分 = Math.min(1, ratio) * 15;
```

> 说明：业务表达中“`min(1, Ratio) × 100 × 15`”代表百分制乘 15% 权重，代码落地应写成 `min(1, Ratio) * 15`，否则趋势分会超过 15 分。

#### 4.4.3 惩罚函数说明

```text
finalScore(norm) = norm × (1 - |norm - 0.5| × 0.67) × 2
```

| 归一化分 | 原始 ×2 后 | 说明 |
| :--- | :--- | :--- |
| 10% | 14.6 | 太少，市场验证不足 |
| 30% | 52.0 | 逐步上升 |
| 50% | 100 | 最优点 |
| 70% | 80.6 | 开始衰减 |
| 100% | 66.8 | 惩罚最重 |

#### 4.4.4 类目基准值（动态）

类目基准值在同步或回算时动态记录，按 `category_id` 维度保存：

1. 类目最大 30 天销量：每个 `category_id` 下的最大 30 天销量。
2. 类目最大 7 天销量：每个 `category_id` 下的最大 7 天销量。
3. 类目最大视频数：每个 `category_id` 下的最大视频数。

第一版可以先在回算任务中实时查询并缓存到内存；后续如果性能不足，再单独建立类目基准值缓存表。

#### 4.4.5 风控规则（触发直接 0 分）

命中以下任一规则时，`selection_score = 0`：

1. 评分 `< 3.0`。
2. 均价 `< $1` 或 `> $100`。
3. 30 天销量 `= 0` 且总销量 `> 0`，判定为疑似刷单。

#### 4.4.6 推荐结论

| 得分 | 结论 |
| :--- | :--- |
| `>= 75` | 推荐上架 |
| `50~74` | 观察等待 |
| `< 50` | 暂不上架 |

#### 4.4.7 全量回算

评分公式修改后，需要跑一次全量回算，重新计算所有产品的 `selection_score`。回算范围覆盖：

1. `fastmoss_product_aggregate`
2. `fastmoss_product_rank_aggregate`
3. `kalodata_youwei_product`
4. `pod_cross_category_product`

### 4.5 手动重算与后台批量重算

“重算”指重新读取数据库中的商品数据，重新计算 `selection_score` 和 `sales_mom`，并回写到数据库。

| 类型 | 触发方式 | 使用场景 |
| :--- | :--- | :--- |
| 手动重算 | 管理员点击按钮或访问内部接口 | 调整评分公式后，立刻验证某一张表或某一天数据 |
| 后台批量重算 | 后台线程定时执行 | 每天新数据入库后自动补齐评分字段 |

第一版建议先做后台批量重算和命令式触发，不急着做复杂前端按钮：

```text
系统启动后，每隔 N 分钟检查最新 date_record 中未评分或评分过期的数据；
每次按 page_size 分批处理，避免一次性锁表；
每条数据独立提交，失败记录错误，不影响整批继续执行。
```

### 4.6 插画状态映射字典

为了避免前后端状态判定混乱，商品表 `illustration_status` 与任务表 `pod_illustration_task.status` 采用同一套枚举字典：

| Status 值 | 商品表含义 | 任务表含义 | 前端展现/交互 |
| :--- | :--- | :--- | :--- |
| `0` | 待处理，未提取过 | 任务排队中 | 显示“提取插画”按钮 |
| `1` | 处理中，已创建任务 | AI 正在生成中 | 显示“提取中...”，不可重复点击 |
| `2` | 已提取成功 | 任务成功完成 | 显示“查看插画”按钮 |
| `3` | 提取失败 | 任务执行失败 | 显示“重试提取”按钮 |
| `4` | 人工废弃/隐藏 | - | 列表不再展示或降低优先级 |

业务流转：

1. 前端发起提取请求后，商品表写入 `illustration_status = 1`，任务表新增记录。
2. 后台任务完成后，把任务状态同步回写到商品表。
3. 成功时写入 `illustration_result_url`；失败时写入任务表 `error_msg`。

---

## 5. 接口与数据契约 (API & JSON Contracts)

本阶段方向 5 暂缓开发。原因是当前跨品类真实数据与插画结果数据尚未完整接入，过早锁定接口结构容易造成返工。

本版本只保留接口设计原则：

1. 继续复用现有 Flask `GET /api/products` 列表接口，通过 `source` 区分 `fastmoss`、`fastmoss_rank`、`kalodata`、`pod_cross_category`。
2. 列表接口只返回轻量字段，禁止返回 `LONGTEXT`、主图 base64、大 JSON 分析字段。
3. 详情接口按需返回大字段。
4. 插画提取接口先保留设计，不作为当前前端必须依赖的验收项。
5. 等跨品类数据真实入库后，再根据列表页实际字段补齐正式 JSON contract。

---

## 6. 前端集成要求 (Frontend Implementation)

修改范围仍为现有 `static/index.html` 和 `static/styles.css`，不引入新的 SPA 框架。

### 6.1 商品列表页展示

在现有表格/卡片中新增与评分相关的展示能力，四类数据源保持一致：

1. 新增“选品分”列或标签，读取 `selection_score`。
2. 新增“趋势”展示，读取 `sales_mom`。
3. 新增“插画状态”展示，读取 `illustration_status`。
4. 评分颜色规则：
   - `selection_score >= 75`：高潜力，绿色
   - `50 <= selection_score < 75`：观察中，黄色
   - `< 50`：低优先级，灰色
5. 当某张表还没有评分字段或字段为空时，展示“未评分”，不能影响原列表加载。

### 6.2 筛选与排序

新增筛选/排序能力建议：

| 控件 | 字段 | 说明 |
| :--- | :--- | :--- |
| 选品分排序 | `selection_score` | 支持升序/降序 |
| 销售环比排序 | `sales_mom` | 支持升序/降序 |
| 插画状态筛选 | `illustration_status` | 待处理、处理中、成功、失败 |
| 高潜力商品筛选 | `selection_score` | 前端按分数区间筛选，例如 `>= 75` |

### 6.3 站内详情页扩展

详情页增加“选品判断”区域：

1. 展示 `selection_score`
2. 展示 `sales_mom`
3. 展示 `score_reason`
4. 展示插画任务状态
5. 如果 `illustration_status = 0`，展示“提取插画”按钮
6. 如果 `illustration_status = 2`，展示原图与提取结果图对比

### 6.4 兼容策略

由于四张表字段不完全一致，前端必须使用统一的字段读取函数，不要在渲染层散落大量 `if source === ...` 判断。建议在前端数据进入渲染前先做一次归一化：

```javascript
function normalizeScoreFields(item) {
  return {
    selectionScore: item.selection_score ?? null,
    salesMom: item.sales_mom ?? null,
    scoreReason: item.score_reason || '',
    illustrationStatus: Number(item.illustration_status ?? 0),
    illustrationResultUrl: item.illustration_result_url || ''
  };
}
```

---

## 7. 文件级改造清单 (File Modification Checklist)

| 文件路径 | 涉及模块 | 修改内容简述 |
| :--- | :--- | :--- |
| `app.py` | 数据源配置 | 在 `SOURCES` 中确认 `pod_cross_category` 映射；保留现有 `fastmoss`、`fastmoss_rank`、`kalodata` 逻辑 |
| `app.py` | 列表查询 | 为四类数据源补充 `selection_score`、`sales_mom`、`illustration_status`、`illustration_result_url` 等轻量字段 |
| `app.py` | 详情查询 | 详情接口补充 `score_reason`、`illustration_result_url` |
| `app.py` | 评分服务 | 新增评分计算函数或 `tools/score_service.py`，支持四张表的字段归一化和批量回写 |
| `app.py` | 后台任务 | 增加后台批量重算逻辑，按表、按日期、分页处理未评分数据 |
| `app.py` | 插画任务 | 新增插画任务创建、状态查询、状态同步逻辑 |
| `static/index.html` | 列表渲染 | 新增选品分、销售环比、插画状态展示；空值显示“未评分” |
| `static/index.html` | 筛选排序 | 新增选品分、销售环比、插画状态相关筛选/排序控件 |
| `static/index.html` | 详情面板 | 新增“选品判断”和“插画素材”区域 |
| `static/styles.css` | 样式 | 增加评分徽标、趋势标签、插画状态按钮、详情对比图样式 |
| 数据库 | 表结构 | 为四张商品表新增 `selection_score`、`score_reason`、`sales_mom`、`illustration_status`、`illustration_result_url` 五个字段 |
| 数据库 | 索引 | 为 `date_record + selection_score`、`date_record + sales_mom`、`illustration_status` 添加索引 |

---

## 8. 验收标准 (Acceptance Criteria)

1. **字段就绪**：`fastmoss_product_aggregate`、`fastmoss_product_rank_aggregate`、`kalodata_youwei_product`、`pod_cross_category_product` 四张表均具备 `selection_score`、`score_reason`、`sales_mom`、`illustration_status`、`illustration_result_url` 五个扩展字段。
2. **索引就绪**：四张表至少具备 `date_record + selection_score`、`date_record + sales_mom`、`illustration_status` 相关索引，列表筛选与排序不出现明显全表扫描。
3. **列表性能**：列表接口默认不查询 `LONGTEXT`、主图 base64、大 JSON 字段；常规第一页响应保持在可交互范围内，深分页需使用延迟关联或等价优化策略。
4. **打分逻辑**：随机抽取每类数据源各 5 个商品，人工复算 `selection_score` 与 `sales_mom`，接口返回结果与公式误差在可解释范围内。
5. **兼容显示**：未评分商品展示“未评分”，不能阻塞原有商品列表、详情、审核状态、链接按钮等现有功能。
6. **插画状态流转**：前端点击提取插画后，`illustration_status` 从 0 流转为 1；任务成功后流转为 2 并展示结果图；失败后流转为 3 并允许重试。
