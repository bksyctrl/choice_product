# 选品审核平台 1.1 版本更新日志

**日期**：2026年5月30日
**核心主旨**：彻底修复前端渲染崩溃问题，并为“站内详情”带来对标商业 SaaS (FastMoss) 的现代化数据看板视觉体验。

---

## 📸 1. 站内详情 UI/UX 深度重构 (图文解析)

本次版本最大的亮点在于“站内详情”弹窗的全面视觉升级。我们抛弃了以前杂乱无章的文本堆砌，改用**高可用性的模块化卡片（Card-based）布局**。

### ① 头部概览区 (Hero Section)

![头部概览区示意图](https://via.placeholder.com/800x200/1e293b/ffffff?text=Hero+Section:+Image,+Title,+and+Tags)

* **左侧视觉锚点**：固定尺寸的商品主图，带有一键跳转原平台（FastMoss / Kalodata）的醒目蓝色主按钮。
* **右侧信息流**：
  - 🍞 **面包屑导航**：清晰展示数据来源（如 `统一表 / 商品详情`）。
  - 📝 **智能截断标题**：商品长标题被限制在最多两行，保证布局不被撑破。
  - 🏷️ **动态标签组**：以灰色圆角小标签的形式，展示 `商品ID`、`采集日期`、`IP等级`、`材质` 等核心属性，层次分明。

### ② 全局基础数据区 (Metric Grid)

![基础数据区示意图](https://via.placeholder.com/800x120/f8fafc/0f172a?text=Metric+Grid:+11+Cards+(Price,+Rating,+Total+Sales,+etc.))

* **独立卡片化**：将原来竖排的 11 项基础数据（价格、评分、佣金比例、人气指数、总销量、总GMV、上架时间等）全部提取出来。
* **视觉强化**：每个指标拥有独立的浅灰色边框卡片，**数字加粗加大**，标签名变小置于上方，让你可以一秒抓取核心业务数据。

### ③ 动态数据总览 (Overview Grid) & 日均小字排版

![数据总览区示意图](https://via.placeholder.com/800x300/ffffff/334155?text=Overview+Grid:+Progress+Bars+&+Grey+Daily+Average+Text)

* **平滑周期切换**：标题旁增加了轻量级的 `近7天 / 近30天` 切换按钮（Tab），点击无缝切换下方的数据周期。
* **左侧 - 成交渠道占比**：
  - 彻底干掉干瘪的文字。
  - 改用**横向视觉进度条（Bar Track）**，通过蓝色填充条的长度，直观对比“视频”、“直播”、“商品卡”三者的销售额转化权重。
* **右侧 - 核心指标（完美实现日均需求）**：
  - 展现周期内的 `销量`、`销售额` 等指标。
  - **✨ 重点细节**：你心心念念的**“日均小字”**完美落地！例如“销售额 $25.93”下方，会紧贴着一行非常精致的灰色极小字体 “日均 $3”，排版极其紧凑，既提供了深度数据，又丝毫不抢夺总数据的视觉焦点。

### ④ 风险与属性深度分析 (Analysis Section)

![风险分析示意图](https://via.placeholder.com/800x250/f0fdf4/0f172a?text=Analysis+Section:+Highlighted+Conclusions+&+List+Format)

* **结论高亮框 (Callout Box)**：为 IP 分析结论增加了一个带品牌色左侧粗边框的模块，将“结论：待复核...”高亮展示，起到警示作用。
* **结构化问答排版**：长篇大论的 AI 分析文本被拆解为：
  1. 插画/图案出自哪里？
  2. 是否构成侵权风险？
  3. 分析原因（从图片、标题、规则三个维度做列表化 `<ul>` 拆解）。
* 极大地降低了运营人员的阅读疲劳感。

---

## 🛠️ 2. 核心底层修复 (Bug Fixes)

除了看得见的漂亮 UI，我们在底层做了一次彻底的“除虫”手术，让你告别“有数据却白屏”的噩梦。

* 🟢 **斩断强制登录的死结**：
  移除了之前意外引入的、会导致无限 404 循环的 `initAuth()` 登录校验拦截。同时，清除了主页面 `#appShell` 容器上残留的 `hidden` 隐藏状态，让页面可以瞬间加载，拒绝白屏。
  
* 🟢 **补全丢失的渲染函数**：
  在代码回滚过程中，找回了三个极其关键但被遗漏的 JavaScript 函数，它们是表格渲染的“心脏”：
  1. **`formatPlainNumber(value)`**：它的任务是精准剥离“佣金比例”等数据末尾多余的 `%` 号，同时保持数字格式的安全。由于之前缺了它，整个表格渲染直接崩溃。
  2. **`statusSelect(item)`**：负责渲染操作列的“状态更改”下拉菜单（待复核、待上架等）。
  3. **`shortText(text, max)`**：防止过长的文本撑爆单元格，智能添加 `...` 省略号。
  
  这三个函数的回归，**彻底治愈了“列表空白且按钮点不动”的绝症**。

---

## 🔒 3. 核心架构状态保证 (Architecture Integrity)

为了保障业务的绝对稳定，我们郑重承诺，本次 1.1 版本更新**严格维持了以下核心架构的现状**，没有任何越界修改：

1. **SQL 纯净度**：`app.py` 中彻底排除了 `latest_by_default=True` 和 `ROW_NUMBER() OVER` 窗口函数等干扰实验，回归最原始稳定的排序与查询逻辑。
2. **过滤引擎完整性**：12 项定制化组合筛选逻辑（含时间范围、佣金、评价、达人数等）运转正常。
3. **前端去重算法防御**：保留了基于采集日期的核心 `dedupeLatestItems` 算法，前端去重机制坚如磐石。